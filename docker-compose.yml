x-app-base: &app_base
  build:
    context: .
    dockerfile: Dockerfile.dev
  restart: unless-stopped
  tty: true
  volumes:
    - .:/var/www
  networks:
    - dash-network

services:
  web:
    <<: *app_base
    container_name: dash-web
    extra_hosts:
      - 'host.docker.internal:host-gateway'
    command: >
      sh -c "php artisan serve --host=0.0.0.0 --port=8000"
    ports:
      - "8000:8000"
    depends_on:
      - cache

  # queues:
  #   <<: *app_base
  #   container_name: dash-queues
  #   command: [ "php", "artisan", "queue:work" ]
  #   depends_on:
  #     - cache

  # scheduler:
  #   <<: *app_base
  #   container_name: dash-scheduler
  #   command: [ "sh", "-c", "while true; do php artisan schedule:run --verbose --no-interaction & sleep 60; done" ]
  #   depends_on:
  #     - web

  frontend:
    image: node:current-alpine
    container_name: dash-node
    restart: unless-stopped
    working_dir: /var/www
    volumes:
      - .:/var/www
    user: "1000:1000"
    networks:
      - dash-network
    command: >
      sh -c "npm install && npm run dev"
    ports:
      - "5173:5173"
    depends_on:
      - web

  db:
    image: postgres:15
    container_name: dash-db
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: dash
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: secret
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - dash-network

  db_dusk:
    image: postgres:15
    container_name: dash-db-dusk
    restart: unless-stopped
    environment:
      POSTGRES_DB: dash_dusk
      POSTGRES_USER: postgres_dusk
      POSTGRES_PASSWORD: secret_dusk
    volumes:
      - pgdata_dusk:/var/lib/postgresql/data
    networks:
      - dash-network

  cache:
    image: redis:7
    container_name: dash-cache
    restart: always
    networks:
      - dash-network

  chromedriver:
    image: seleniarm/standalone-chromium
    extra_hosts:
      - 'host.docker.internal:host-gateway'
    container_name: dash-chromedriver
    shm_size: 2gb
    ports:
      - "9515:4444"
    networks:
      - dash-network

volumes:
  pgdata:
  pgdata_dusk:


networks:
  dash-network:
    driver: bridge
